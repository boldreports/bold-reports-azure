INSERT INTO BOLDRS_Source (Name, IsActive) VALUES ('Viewer', 1);

CREATE TABLE BOLDRS_FileDetail (
    Id NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
    FileId VARCHAR2(36) NOT NULL,
    Password VARCHAR2(255) NOT NULL,
    ModifiedDate TIMESTAMP NOT NULL,
    IsActive NUMBER(5) NOT NULL
);

ALTER TABLE BOLDRS_FileDetail ADD CONSTRAINT fk_filedetail_fileid FOREIGN KEY (FileId) REFERENCES BOLDRS_Item (Id);
