
@{
    var globalAppSettings = _globalAppSettings;
    ViewBag.Title = globalAppSettings.SystemSettings.OrganizationName + " - [[[Report Not Found]]]";
    ViewBag.MessageBox = false;
    var reportServerResourceUrl = globalAppSettings.SystemSettings.CDNLink.TrimEnd('/');
}
@if (Layout == null)
{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section scripts{
    <environment include="Development,CloudDevelopment">
        <script asp-append-version="true" src="@reportServerResourceUrl/cdn/scripts/bootstrapjs.js"></script>
    </environment>
    <environment exclude="Development,CloudDevelopment">
        <script asp-append-version="true" src="@reportServerResourceUrl/cdn/scripts/bootstrapjs.min.js"></script>
    </environment>
}
@section MainSection{
    <div>
        <div class="col-md-12">
            <div class="col-xs-12 text-center response-header">[[[Sorry, we could not find your report.]]]</div>
            <div class="response">[[[Possible reason:]]]<br>[[[The report could have been deleted.]]]</div>
        </div>
    </div>
}
